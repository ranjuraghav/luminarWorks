var low_limit=10,upper_limit=50;
// print even numbers b/w low to upper

while (low_limit<=upper_limit) {
    if(low_limit%2==0){
        console.log(low_limit);
    }
    low_limit ++;
}

var low_limit=100,upper_limit=50;
if(low_limit>upper_limit){
    console.log(`pls enter crct value for low limit`);
}
else{
    
while (low_limit<=upper_limit) {
    if(low_limit%2==0){
        console.log(low_limit);
    }
    low_limit ++;
}


}